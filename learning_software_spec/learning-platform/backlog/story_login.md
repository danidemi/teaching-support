As:
an `unregistered user` 

I want to: 
sign in to the platform with a Google account

So that:
* I will be able to use the services that will be provided, i.e. to upload a quiz I prepared to allow my students to access it and take it

Definition of Done:
* as an `unregistered user` I can open the browser, type the platform URL, find a sign in/log in "button", click it, access the system using Google account.
* I become a `registered user`