As:
an `unregistered user` 

I want to: 
to start to use the `learning platform`

So that:
use one of the services that will be provided, i.e. to upload a quiz I prepared to allow my students to access it and take it

Definition of Done:
as an `unregistered user` I can open the browser, type the platform URL, sign in into the system using Google account.
