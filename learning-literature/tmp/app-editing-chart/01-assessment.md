agent learning-curriculum-architect builds a graph representing the logical constraints of topics to be presented during an adult course.
the agent stores the graph in a markdown file, using markdown tables to represents nodes, their properties, edges and their properties.

this approach is not feasible because humans have difficulties in understanding a graph described in this way.

I need an app that allows a human to graphically check the graph, add, remove, modify nodes, add, remove, modify edges and store the graph.
this will allow the human to check the grpah proposed by the artificial intelligence and to certify a final version of the model other agents will be able to use for later tasks.

the markdown format to stroe the graph seems not efficient too.

so, don't write any code, just propose me a techonolgical stack to realize the app I described you.
several attention points
- it should be an application usable from browser
- the format and the storage of the model. is md the righ format? is json better? would an embeddable graph database better? I suppose a course is not made up by many nodes so maybe a graph theoru related library should be enough.

Grill my specs, make questions on the topic you don't understand, put me pressure.

eventually describe me the stack and an overview of how the app will work.